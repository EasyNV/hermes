# deploy/secrets/prod/.gitignore — never commit production secret bytes.
#
# Whitelist only the .example placeholders. Everything else under this
# directory stays untracked.

*
!.gitignore
!*.example
!README.md
